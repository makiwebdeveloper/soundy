"use client";

import { AudioFileType } from "@/types/tracks.types";

interface Props {
  tracks: AudioFileType[];
}

export default function UploadAlbumForm({ tracks }: Props) {
  return <div>upload-album-form</div>;
}
